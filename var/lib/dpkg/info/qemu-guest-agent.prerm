#!/bin/sh
set -e
# Automatically added by dh_installinit/13.3.4
if [ -x "/etc/init.d/qemu-guest-agent" ] && [ "$1" = remove ]; then
	invoke-rc.d --skip-systemd-native qemu-guest-agent stop || exit 1
fi
# End automatically added section
